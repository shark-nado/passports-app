services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./passports.db:/app/passports.db
    environment:
      - JWT_SECRET=change-me-in-production-to-a-32-byte-key!
      - DATABASE_URL=sqlite+aiosqlite:///app/passports.db
